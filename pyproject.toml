[project]
name = "build-ios"
version = "0.1.0"
description = "Build a Godot Project for iOS without a Mac"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dropbox~=12.0",
    "inquirer~=3.4",
    "python-dotenv~=1.2",
]
